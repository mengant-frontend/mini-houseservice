<view class="component">
  <textarea class="textarea f-class" value='{{local_value}}' bindinput='bindInput' maxlength='{{maxLength}}' placeholder="{{placeholder}}"></textarea>
  <view wx:if='{{show_total && maxLength !== -1}}' class="count">{{len}}/{{maxLength}}</view>
</view>