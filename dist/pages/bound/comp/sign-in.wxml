<view class="bg" style="height: {{height}}rpx"></view>
<view class="sign-in-container">
    <view class="btn" wx:if="{{sign_in === false && !show}}" bindtap="login">
        <text>签到</text>
    </view>
    <view class="sign-in">
        <view class="sign-in-desc">
            <text>可用积分</text>
        </view>
        <view class="sign-in-bound">
            <text>{{score}}</text>
        </view>
    </view>
</view>
