/* 该组件的使用场景应该与 body 节点级别一样是撑开整个页面的，
tabs 栏固定顶部，内容区域使用 swiper 支持横向滑动切换；
所以组件使用者需要给页面这样设置样式：
page {
  height: 100%;
  padding-top: 42px; // i-tabs 组件的高度
  box-sizing: border-box; // 作为内容区域的 swiper 将会继承页面剩下的高度
} */
.swiper {
  height: 100%;
}
.swiper-item {
  overflow: auto;
}
